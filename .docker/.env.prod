# Looms Production Environment
# Used with: docker compose --profile prod up
#
# Runs its own Dolt server (fully isolated).

# App
LOOMS_PORT=3000
REALTIME_PORT=5199
LOG_LEVEL=warn

# Dolt (containerized, isolated)
DOLT_DATABASE=beads_looms
DOLT_USER=root
DOLT_PASSWORD=
